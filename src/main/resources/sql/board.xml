<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.devstudy.mapper.BoardMapper">

	<sql id="boardSearchCondition">
	    WHERE boardType = #{boardType}
	
	    <if test="keyword != null and keyword != ''">
	        <choose>
	            <when test="searchType == 'title'">
	                AND title LIKE #{searchPattern} ESCAPE '!'
	            </when>
	            <when test="searchType == 'content'">
	                AND content LIKE #{searchPattern} ESCAPE '!'
	            </when>
	            <otherwise>
	                AND (
	                    title LIKE #{searchPattern} ESCAPE '!'
	                    OR content LIKE #{searchPattern} ESCAPE '!'
	                )
	            </otherwise>
	        </choose>
	    </if>
	</sql>

    <select id="selectBoardList"
            parameterType="com.devstudy.vo.BoardVO"
            resultType="com.devstudy.vo.BoardVO">
        SELECT boardIdx,
               boardType,
               category,
               memberIdx,
               title,
               content,
               viewCount,
               createdAt,
               updatedAt
        FROM board
	        <include refid="boardSearchCondition"/>
	    ORDER BY boardIdx DESC
	    LIMIT #{pageSize} OFFSET #{offset}
    </select>
    
    <select id="selectBoardCount"
	        parameterType="com.devstudy.vo.BoardVO"
	        resultType="long">
	    SELECT COUNT(*)
	    FROM board
	    <include refid="boardSearchCondition"/>
	</select>
    
    <insert id="insertBoard"
	        parameterType="com.devstudy.vo.BoardVO"
	        useGeneratedKeys="true"
	        keyProperty="boardIdx">
	
	    INSERT INTO board (
	        boardType,
	        category,
	        memberIdx,
	        title,
	        content
	    ) VALUES (
	        #{boardType},
	        #{category},
	        #{memberIdx},
	        #{title},
	        #{content}
	    )
	
	</insert>
	
	<select id="selectBoard"
	        parameterType="com.devstudy.vo.BoardVO"
	        resultType="com.devstudy.vo.BoardVO">
	    SELECT boardIdx,
	           boardType,
	           category,
	           memberIdx,
	           title,
	           content,
	           viewCount,
	           createdAt,
	           updatedAt
	    FROM board
	    WHERE boardIdx = #{boardIdx}
	</select>
	
	<update id="updateBoard"
	        parameterType="com.devstudy.vo.BoardVO">
	    UPDATE board
	    SET category = #{category},
	        title = #{title},
	        content = #{content},
	        updatedAt = CURRENT_TIMESTAMP
	    WHERE boardIdx = #{boardIdx}
	      AND memberIdx = #{memberIdx}
	</update>
	
	<delete id="deleteBoard"
	        parameterType="com.devstudy.vo.BoardVO">
	    DELETE FROM board
	    WHERE boardIdx = #{boardIdx}
	      AND memberIdx = #{memberIdx}
	</delete>
	
	<select id="selectBoardForUpdate"
	        parameterType="com.devstudy.vo.BoardVO"
	        resultType="com.devstudy.vo.BoardVO">
	    SELECT boardIdx,
	           boardType,
	           memberIdx
	    FROM board
	    WHERE boardIdx = #{boardIdx}
	    FOR UPDATE
	</select>
	
	<select id="selectFaqList" resultType="com.devstudy.vo.FaqVO">
	    SELECT faqIdx,
	           question,
	           answer
	    FROM faq
	    WHERE isVisible = 1
	    ORDER BY sortOrder ASC, faqIdx DESC
	</select>

</mapper>